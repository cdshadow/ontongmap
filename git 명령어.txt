cd D:\d\01_code\py\ontongmap   # ontongmap 폴더로 이동

# git init # 맨 처음에만. 깃 등록.

git status   # 현재 변경사항 확인

git add .   # 현재 폴더의 모든 파일을 스테이징

git commit -m "update ontongmap_20260902"   # 변경사항 커밋

git push -u origin main --force   # GitHub main 브랜치를 현재 로컬 main 상태로 강제 덮어쓰기